# Progress Log

Agent run log — updated via `/track`. This is NOT the PM/schedule truth (see CLAUDE.md and constitution.md §7); the Notion Sprint Board is. On conflict, Sprint Board wins.

## 2026-07-12 — Repo scaffolded

- Applied the adjusted repo structure from the "SDD Repo Setup Guide — Exclusive Venue" (Notion).
- Seeded stack-profile.md, constitution.md, design-system/INTAKE.md, fixtures/README.md.
- Seeded specs/0000-foundation/erd.md from the Notion ERD doc; rls-matrix.md, route-architecture.md, api-contracts.md left as placeholders pending their source Sprint Board tasks (route-architecture, api-contracts land Week 1–2 per setup guide §2 step 9).
- Seeded specs/0001-product-1-core/ (product-brief.md, prd.md, verification.md) from the Notion PRD; tasks.md seeded from setup guide §8 (A1–J2).
- Next: run `/constitution` to validate; begin Week 1 schema task (A1) once repo is reviewed.

## 2026-07-12 — A1 + A2 implemented

- Scaffolded `api/` (FastAPI, Python 3.12): `app/main.py` health check, `app/core/config.py` (env-driven settings), `app/core/auth.py` (Supabase JWT staff-session verification for A2), `requirements.txt`, `pyproject.toml`.
- Wrote `migrations/versions/0001_profiles_user_roles.py`: `profiles`, `user_roles`, `has_role()` helper (SECURITY DEFINER, used by every later RLS policy), RLS enabled + policies on both tables per `rls-matrix.md` (staff/admin ALL, self SELECT). Hand-written per `docs/stack-profile.md` (RLS/CHECK intent, not autogenerate). **Not yet applied** — no live Supabase instance connected in this environment; needs `SUPABASE_DB_URL` + `alembic upgrade head`.
- Scaffolded `web/` (Next.js App Router, TS strict): root layout + public Concierge placeholder (`/`, Tier-1-safe, no real content per `design-system/INTAKE.md`), `web/middleware.ts` (session + `has_role` gate on `/app/*` per `route-architecture.md`), `/app/login` (Supabase Auth password sign-in), `/app` Pipeline Board placeholder (real board is H2). Supabase client helpers in `web/lib/supabase/`.
- Marked A1/A2 done in `tasks.md` with caveats (dependencies not installed, not run against a live DB — no network/Supabase project in this environment). Next actionable task once a Supabase project + `.env` values exist: run `alembic upgrade head`, `npm install && npm run dev`, create a staff user + `user_roles` row via the Supabase dashboard, verify login end-to-end. Then continue to **B1** (venue schema + CRUD API).

## 2026-07-12 — A1 verified against live Supabase project

- Project connected: `byupzaqlyvwssfqgryng.supabase.co`. Two fixes needed to actually reach it from this dev environment: (1) Supabase's newer key system (`sb_publishable_...`/`sb_secret_...`, JWKS-based) has no shared JWT secret, so `api/app/core/auth.py` was rewritten to verify sessions via the Supabase Auth API (`auth.get_user(token)`) instead of a local HS256 decode — `config.py` updated (`supabase_secret_key`, `supabase_jwks_url`, dropped `supabase_jwt_secret`); `pyjwt` dropped from `requirements.txt`. (2) Supabase's direct DB host (`db.<ref>.supabase.co`) is IPv6-only (free tier dropped IPv4 in 2024) and unreachable from this machine — switched to the **session pooler** (`aws-0-eu-central-1.pooler.supabase.com:5432`, IPv4); `migrations/env.py` now also normalizes `postgresql://` → `postgresql+psycopg://` so the URL works with the `psycopg` v3 driver actually installed.
- `pip install -r api/requirements.txt` into a repo-root `.venv`, then `alembic upgrade head` — ran clean, `Running upgrade -> 0001`.
- Verified live: `profiles` + `user_roles` tables exist, `has_role()` + `set_updated_at()` functions exist, RLS enabled on both tables, all 4 expected policies present (`profiles_staff_all`, `profiles_select_own`, `user_roles_staff_all`, `user_roles_select_own`).
- Not yet done: `web/` deps not installed (`npm install`), no staff user/`user_roles` row created yet, login flow not exercised end-to-end. Next: `cd web && npm install && npm run dev`, create a staff user in the Supabase dashboard + insert its `user_roles` row, log in at `/app/login`. Then continue to **B1** (venue schema + CRUD API).

## 2026-07-12 — web/api dev servers up, hydration fix, B1 shipped + live-verified

- `npm install` in `web/` (one transient network retry, otherwise clean); both dev servers booted (`api` :8000ish, `web` :3000) and confirmed serving: `/` 200, `/app/login` 200, `/app` 307-redirects to login per `middleware.ts`.
- Fixed a hydration mismatch on `/app/login`: added `autoComplete` + `suppressHydrationWarning` on the email/password inputs — the standard fix for password-manager browser extensions injecting attributes into auth-form inputs before React hydrates.
- **B1 shipped and live-verified.** `migrations/versions/0002_venue_domain.py`: `venues`, `venue_configurations`, `venue_media`, `venue_availability`, `venue_restrictions` + RLS per `rls-matrix.md`. `venues.hero_media_id` FK to `venue_media` added via `ALTER TABLE` after both tables exist (breaks the circular reference). Applied clean: `alembic upgrade head` → `0001 -> 0002`. Verified live: all 5 tables, RLS enabled on all, all expected policies present, `hero_media_id` FK present.
- Built `api/app/routers/venues.py` (venues CRUD + nested configurations/restrictions/availability) and `api/app/schemas/venue.py` (Pydantic validation mirroring the DB CHECK constraints). New `api/app/core/scoped_client.py`: every request runs against a Supabase client scoped to the caller's own JWT (publishable key + their access token), not the secret key — RLS is what actually authorizes each row, the router only shapes requests/responses (constitution #1 trust boundary).
- **Real bug found and fixed along the way:** `supabase-py` 2.9.1 client-side-validates API keys as 3-segment JWTs and throws `Invalid API key` on this project's newer `sb_secret_.../sb_publishable_...` format. Upgraded to `supabase==2.31.0` in `requirements.txt`.
- Smoke-tested the full CRUD flow against the live DB with a throwaway staff account (created + role-granted with explicit user confirmation, deleted afterward): create/read/update venue, create configuration/restriction/availability-hold, a hold with no `hold_expires_at` correctly rejected (400, CHECK constraint), `/approve` correctly flips status to `active`, delete + 404-after-delete confirmed. DB confirmed empty again after cleanup (`user_roles`: 0, `venues`: 0).
- **Operator error, disclosed to the user:** while clearing a stuck dev-server port, a stale/misattributed netstat PID entry led to force-killing the user's actual VS Code process. No files were affected; flagged immediately.
- Not yet done: B2 (media storage), B3-B6 (venue UI). Next: **B2** (Supabase Storage bucket + signed URLs) or **B3** (Venue Library/Profile UI) — either can start now that B1's API exists.

## 2026-07-12 — B2 shipped + live-verified

- Created the private `venue-media` Supabase Storage bucket via `scripts/create_storage_buckets.py` (idempotent, run once per environment).
- Built `api/app/core/storage.py` (admin-scoped upload/signed-url/delete against the private bucket) and `api/app/routers/venue_media.py`: upload/list/update/delete on `venue_media`. Authorization order matters here — the `venue_media` row is inserted via the caller's RLS-scoped client *first*; the file only gets written to Storage after that insert succeeds, so a denied DB write never results in an orphaned object. On a storage-upload failure after a successful DB insert, the row is rolled back rather than left dangling.
- Live-verified: uploaded a real file, got back a working signed URL (fetched it directly and confirmed the byte count matched exactly what was sent), updated caption metadata, deleted (confirmed removed from both the `venue_media` table and the storage bucket), and confirmed venue delete cascades correctly.
- Repeated the throwaway-staff-account smoke-test pattern from B1 (explicit user confirmation before granting the role, deleted afterward).
- **Recurring issue, now worked around rather than repeated:** `uvicorn --reload`'s WatchFiles reloader keeps missing changes when multiple files land in the same debounce window (seen on both A1→A2 and B1→B2) — it only reloads on the first detected file, silently serving stale code afterward. After the earlier VS Code process-kill incident, further attempts to force-kill stale server processes were correctly blocked by the permission system even with verified command-line matches. Workaround adopted: stop trying to kill old dev-server processes at all; when reload state is suspect, just start a fresh instance on a new port. Currently running: api on port 8030 (no `--reload`, to sidestep the debounce bug entirely), web on port 3000. Ports 8000/8010/8020 likely still have stale zombie processes from earlier in this session — harmless, just ignore them; do not attempt to kill anything on those ports.
- Not yet done: B3-B6 (venue UI — Library, Profile, create/edit form, media gallery, calendar). Next: **B3** (Venue Library + Profile pages).

## 2026-07-12 — B3 shipped + browser-verified (still Product 1)

- Built `web/lib/api/client.ts` (RLS-scoped fetch wrapper — attaches the caller's own Supabase session token, same trust-boundary pattern as the API side) and `web/lib/api/types.ts`.
- `web/app/app/venues/page.tsx` (Venue Library: search by name/district/slug, status filter, table) and `web/app/app/venues/[id]/page.tsx` (Venue Profile: details, configurations, restrictions, read-only media thumbnails, staff Approve action gated the same way the API gates it — RLS, not a client-side role check). Added a minimal staff nav (`web/app/app/layout.tsx`, hidden on `/app/login`) so Venues is actually reachable from the Pipeline Board.
- **Real gap found and fixed**: the API had no CORS policy, so the browser silently blocked every cross-origin call from `web/` (:3000) to `api/` (:8030+) — invisible to `curl`-based testing, only surfaced once an actual browser hit it. Added `CORSMiddleware` to `api/app/main.py`, dev-only origin allowlist.
- **Also fixed two pre-existing strict-mode TS errors** surfaced by running `tsc --noEmit` for the first time this session (implicit `any` on `cookiesToSet` in both `web/lib/supabase/server.ts` and `web/middleware.ts`, left over from A2 scaffolding) — typed against `CookieOptions` from `@supabase/ssr`.
- **Verified with an actual browser, not just curl**: installed Playwright + Chromium, drove the real login form with a throwaway staff account, confirmed the Venue Library renders a live venue (correct name/district/status/ownership label) and the Venue Profile renders its configuration and restriction — zero console errors. This caught the CORS bug that `curl`/`httpx` testing structurally could not have caught (no browser = no CORS enforcement).
- API is now running on port 8040 (yet another new port — the fresh-instance-per-suspect-reload pattern from B2 continues; `web/.env.local`'s `NEXT_PUBLIC_API_URL` updated to match). Test venue, test staff account, and role grant all cleaned up afterward (confirmed `user_roles`: 0, `venues`: 0).
- Not yet done: B4 (create/edit form), B5 (media gallery upload/reorder UI), B6 (availability calendar UI). Next: **B4** (venue create/edit form) — natural next step since B3 has nowhere to send someone wanting to add a venue yet.

## 2026-07-12 — dev-server port sprawl cleaned up

- The port-hopping pattern from A2/B2/B3 (8000→8010→8020→8030→8040) was the wrong response to an infrequent `--reload` debounce quirk — the right response was just "stop the server and restart it," not "start a second one on a new port and never clean up the first." Root-caused and corrected: `README.md` now documents a single-instance, fixed-port workflow (`api/` always :8000, `web/` always :3000).
- `web/.env.local`'s `NEXT_PUBLIC_API_URL` reset to the standard `http://localhost:8000`.
- Process cleanup was attempted but the permission system correctly refused a broad kill after the earlier VS Code incident, even with verified command lines — user is restarting the machine instead, which also clears a stale/ghost TCP listener entry on port 8000 itself (same phantom-PID pattern seen earlier in the session; not a real process, just stuck OS socket-table state that only a restart clears).
- Next session should start exactly one `uvicorn app.main:app --reload --port 8000` and one `npm run dev`, confirm both, and continue with **B4**.

## 2026-07-13 — B4 shipped + browser-verified

- **`/app/venues/new`** (create form: name, auto-slug, district, address, description) and **`/app/venues/[id]/edit`** (edit those fields + add/remove configurations + add/remove restrictions inline). No API changes needed — B1's CRUD endpoints already covered all of this. Added `VenueCreate`/`VenueUpdate`/`VenueConfigurationCreate`/`VenueRestrictionCreate` types + `RESTRICTION_KINDS` to `web/lib/api/types.ts`. "+ New venue" link on Venue Library, "Edit" link on Venue Profile.
- Design: Tier 1 provisional tokens only (`design-system/tokens.provisional.css`) — this is a staff-only internal surface, same tier as B3. Brand kit (Tier 2) still not accepted; a reskin pass is out of scope until it lands.
- **Introduced a persistent dev/test staff account** (`test@user.com`, seeded via new `scripts/create_test_staff_user.py`, idempotent, same pattern as B2's storage-bucket script) instead of the throwaway-account-per-session pattern from B1–B3 — the account already existed from a prior session, so the script just confirmed the `staff` role grant and made zero changes. Credentials noted in `web/.env.local` (gitignored) for reuse.
- Browser-driven (Playwright): logged in with the persistent test account, created a venue, added a configuration and a restriction, edited the name, saved, confirmed everything rendered correctly back on the Venue Profile page. Zero console errors. Test venue deleted afterward (DB confirmed clean of test data).
- `tsc --noEmit` clean (one `noUncheckedIndexedAccess` fix needed: a `RestrictionKind[0]` array-index default swapped for a literal).
- Not yet done: B5 (media gallery upload/reorder), B6 (availability calendar). Next: **B5** or **B6** — both only depend on B1/B2, either can start now.

## 2026-07-13 — B5, B6 shipped; then C1 through H1 (full backend build-out)

User asked to continue building out all remaining Product 1 APIs without stopping to ask each time. Summary of a long session — see `tasks.md` for the per-task detail this entry doesn't repeat.

- **B5 (media gallery upload/reorder)** and **B6 (availability calendar)**: both added to the Venue Profile / edit pages, reusing B1/B2's existing API — no backend changes needed. Browser-verified together (Playwright): upload two photos, reorder them, add a hold block to the calendar, zero console errors.
- **Build order deviated from tasks.md's letter order once, deliberately**: F1/F2/F3 (pricing engine) were built *before* E1 (recommendation filter), because E1's "budget fit" criterion genuinely needs the pricing engine to exist first to compute a real quote rather than a crude heuristic. Documented inline in tasks.md.
- **C1** (organisations/contacts/enquiries, revision 0003) → **D1/D2/D3** (briefs, revision 0004, Claude tool-use parser + confidence threshold + golden harness) → **F1/F2/F3** (pricing_rules/addons, revision 0005, deterministic calculator, 17 unit tests) → **E1/E2** (deterministic filter + Claude re-rank, reusing F2) → **G1** (proposals/proposal_venues/proposal_link_tokens, revision 0006, plus a public token-reader endpoint standing in for the edge function this repo has no scaffolding for) → **H1** (enquiry stage machine + assignment).
- **Every migration (0003-0006) applied to the live Supabase project and verified** — tables, RLS policies, and CHECK constraints all confirmed live, not just reviewed on paper.
- **Every endpoint live-smoke-tested** end to end (not just unit tests) using the persistent `test@user.com` staff account: enquiry/brief/pricing/shortlist/proposal/stage-transition flows all exercised against the real API + DB, with test data cleaned up after each run (DB confirmed empty of test rows at the end of the session).
- **Known gap, disclosed rather than hidden: no `ANTHROPIC_API_KEY` configured in this dev environment.** The brief parser (D1), re-rank (E2), and their tests are built and structurally verified (missing key → clean 503, not a crash; golden test harness skips rather than fails), but the actual Claude tool-use calls have never run live. Needs a real key + a live pass before D1/D2/D3/E2 are considered fully verified, not just "should work."
- **Bug caught by its own test, fixed same session**: the stage machine's terminal-stage handling initially let `confirmed` transition to `lost` (a "lost" reachability loop was applied to every non-`lost` stage instead of every non-terminal stage) — `test_confirmed_is_terminal` caught it immediately, one-line fix.
- **UI-only tasks deliberately left undone** (D4 review UI, F4 rules editor tab, F5 seed script, G0/G2/G3/G4/G5 proposal builder UI, H2 kanban board, J1/J2): the user's ask was specifically "get all the APIs done," and every one of these already has the API surface it needs (briefs PATCH, pricing CRUD, proposals CRUD + public token reader) — building the UI is the natural next phase, not blocked on anything backend.
- Dev servers: API restarted fresh on port 8000 multiple times this session (the known `--reload` WatchFiles debounce bug recurred whenever two routers landed in the same save window) — each restart confirmed via `tasklist` that the PID being killed was our own `python.exe` before touching it, per the VS Code incident lesson from earlier in the project.
- Next: pick up the UI-only tasks above (G0 unified enquiry command center is probably the highest-leverage next step — it's the page D4/E2/F2's outputs actually get used from), or add a real `ANTHROPIC_API_KEY` and do a live pass on D1/D3/E2 first.

## 2026-07-13 — AI provider switched: Anthropic → OpenAI

At user request, swapped the product's runtime AI from Claude (Anthropic) to GPT (OpenAI, `gpt-4o-mini`) — scoped to the runtime call path only, not the coding-assistant tooling (`CLAUDE.md`/`.claude/` deliberately untouched; those configure Claude Code, the dev tool, which has nothing to do with which LLM the deployed product calls).

- `api/app/core/claude.py` → `api/app/core/openai_client.py` (`get_openai_client()`, still `PARSER_MODEL = "gpt-4o-mini"`).
- `api/app/services/brief_parser.py` (D1) and `recommendation_engine.py`'s `rerank_shortlist` (E2) rewritten for OpenAI's `chat.completions.create(tools=..., tool_choice=...)` shape instead of Anthropic's `messages.create(tools=..., tool_choice=...)` — same forced-tool-call pattern, different response envelope (`response.choices[0].message.tool_calls[0].function.arguments`, a JSON string, vs Anthropic's typed content blocks).
- `requirements.txt`: `anthropic==0.39.0` → `openai==2.45.0`. `api/.env` / `.env.example`: `ANTHROPIC_API_KEY` → `OPENAI_API_KEY`. `config.py`: `anthropic_api_key` → `openai_api_key`.
- Routers (`briefs.py`, `recommendations.py`): `anthropic.APIError` → `openai.APIError` in the 502 exception handlers.
- Tests updated: `test_recommendation_engine.py`'s fake-client monkeypatch rebuilt to mimic OpenAI's response shape (chat/completions/message/tool_calls, not Anthropic's content blocks); `test_golden_briefs.py`'s skip condition now checks `openai_api_key`.
- Docs updated to say GPT/OpenAI instead of Claude/Anthropic: `memory/constitution.md` (trust boundary rule #1), `docs/stack-profile.md`, `specs/0001-product-1-core/prd.md`, `specs/0000-foundation/api-contracts.md`, `specs/0001-product-1-core/tasks.md` (D1/E2/G2 descriptions, with a note on the switch left in D1's entry).
- Full pytest suite re-run clean (42 passed, 1 skipped — golden set still has no live key to run against). API server restarted fresh and the D1 smoke test re-run: the missing-key 503 now correctly reads "OPENAI_API_KEY is not configured" instead of the old Anthropic message — confirms the swap end-to-end at the plumbing level, though **the actual GPT calls are still untested live** (same caveat as before the swap, just a different provider now).
- **Not done, out of this session's reach: the Notion Sprint Board / PRD source-of-truth docs.** No Notion access/integration is available in this environment — the local `prd.md`/`api-contracts.md` copies were updated (reversing the normal "Notion is source of truth" flow), so Notion itself needs a manual edit to match, or it'll silently drift from the repo. Flagged to the user directly in-conversation with a copy-paste-ready summary.
- Next: get a real `OPENAI_API_KEY` into `api/.env` and do a live pass on D1 (parser), D3 (golden set), and E2 (rerank) — none of the AI call paths have ever actually executed against a live model, under either provider.

## 2026-07-15 — Remaining Product 1 UI shipped (C3, D4, F4, F5, G0, G2, G3, H2, J1, J2)

User asked to continue spec development. This closes out essentially all of the remaining UI-only tasks from `tasks.md`, on top of last session's API-only work, using the already-existing endpoints (only one schema-adjacent change: `ShortlistEntry` gained `pricing_rules_id`/`quote_breakdown`).

- **Pipeline Board (H2)** at `/app` replaces the placeholder: kanban columns by stage, cards pulling event type/guests/date/budget from each enquiry's latest brief, "+ New enquiry" opens **C3**'s modal (`web/app/app/NewEnquiryModal.tsx` — optionally creates an organisation + contact first).
- **Unified enquiry command center (G0 + D4)** at `/app/enquiries/[id]`: raw message, stage-transition buttons + assignment, an editable brief form (parse/re-parse/edit/approve — degrades to a clear notice rather than a crash when `OPENAI_API_KEY` is missing), and a shortlist section (`GET /briefs/{id}/shortlist`, optional AI re-rank) with a "Create proposal from shortlist" action. This single page absorbed H2's "enquiry detail" requirement too.
- **F4** (pricing rules tab) and **F5** (seed script) shipped together: `PricingRulesTab.tsx` inside the Venue edit page (simplified entry surface over the jsonb rule shapes: one per-head rate, Friday/Saturday multipliers, included-hours+overtime, min spend, add-ons, plus a live "test quote" calculator), and `scripts/seed_demo_venues.py` — run live, seeding 3 demo venues (Peak Skyline Hall, Wan Chai Warehouse Loft, Repulse Bay Garden Pavilion) with configs/restrictions/pricing/add-ons. These are intentionally left in the DB as standing demo/QA fixtures, unlike this session's throwaway enquiries/proposals.
- **E1 extended**: `ShortlistEntry` now carries `pricing_rules_id` + the full `quote_breakdown` when a rule matched, so G0's "Create proposal" button can build `proposal_venues` rows directly from the shortlist without a second pricing lookup. 2 new unit tests.
- **G2** (copy generation): `app/services/copy_generator.py`, plain GPT completion (not tool-calling — this is prose). Two endpoints, output lands in the same plain editable columns G1 already had.
- **G3** (proposal editor) at `/app/proposals/[id]`: intro copy + per-venue cards (quote, editable copy, recommended toggle, remove) + send + shareable-link create/revoke.
- **J1** (clients) and **J2** (portfolio calendar): read-only, built entirely from existing tables, no schema changes.
- **Real bug found and fixed**: `web/lib/supabase/client.ts` created a brand-new Supabase client on every `apiFetch()` call. Modern supabase-js serializes session access across client instances via the browser's Web Locks API — harmless for pages making 1-2 sequential calls, but the proposal editor (G3) is the first page to fire 3 in a row (proposal → venues → links), and that was enough to hit real lock contention: the 2nd/3rd call hung forever with no error, no console message, nothing — venues silently stayed empty. Root-caused by adding temporary timing instrumentation to `apiFetch` and watching exactly where the promise chain stalled, then confirmed against uvicorn's request log (the OPTIONS preflight for `/venues` succeeded but the real GET never arrived) before finding the actual cause. Fixed by making `createClient()` a module-level singleton — supabase's own recommended pattern. This bug was latent in every page built all session; it just needed 3+ sequential `apiFetch` calls on one page to surface, which nothing before G3 did.
- Every new page browser-verified with Playwright against either live-created test data (cleaned up after) or the seeded demo venues. Full pytest suite: 44 passed, 1 skipped (golden set, still no API key).
- Test data cleanup confirmed: all throwaway enquiries/proposals/contacts removed; only the 3 intentional seed venues remain.
- Not built this session (deliberately, still UI-only and lower priority): C2 (public enquiry form), C4 (Resend webhook), G4 (PDF export — Tier-2 gated), G5 (public proposal page — Tier-2 gated, but its data endpoint already exists from G1), I1-I3 (CI wiring / E2E / verify sign-off).
- Next: either get a real `OPENAI_API_KEY` and do the first live pass on the parser/re-rank/copy-generation call paths, or continue with I1 (wire the test suite into `ci.yml`) to close out the verification group.

## 2026-07-15 (continued) — C4 (Resend webhook) + I1 (CI wiring made real) + a genuine Supabase bug fix confirmed

User followed up mid-session with "and fix the supabase issue" — confirmed the singleton fix from the previous entry is complete and correctly scoped (only `web/lib/supabase/client.ts`'s browser factory needed it; the two call sites — `login/page.tsx`, `lib/api/client.ts` — inherit the fix automatically; `lib/supabase/server.ts`'s per-request SSR client is a different, correct pattern, left untouched). Re-ran a quick Playwright sanity check post-fix: zero console errors.

- **C4** (Resend inbound webhook): `POST /webhooks/resend/inbound-email`. Svix-scheme HMAC signature verification (`app/services/webhook_verification.py`) gates every byte before trust — fails **closed** (503/401) on a missing secret or bad signature, the opposite of D1/E2's fail-soft pattern, because this is an unauthenticated public endpoint, not a staff action. 5 unit tests (self-signed payloads, no real Resend account involved). Live-verified against a throwaway instance with a real signed payload: contact + enquiry created correctly, tampered body correctly 401'd, cleaned up after. **Payload shape is unverified against a live Resend account** — written from docs, not a captured real webhook; flagged clearly in `tasks.md`.
- **I1** (CI wiring) turned out to be more than flipping a checkbox — the existing `ci.yml` had two real gaps that would have failed on the very first run:
  1. Its Postgres service is plain `postgres:16`, which has no `auth` schema — every migration since 0001 references `auth.users`/`auth.uid()` (only real in the live Supabase project). Added a bootstrap step that stubs a minimal `auth.users` table + a null-returning `auth.uid()`, just enough for `alembic upgrade head` to apply cleanly. Reasoned through carefully but **not run against real GitHub Actions** — no way to spin up an actual workflow from this environment.
  2. `web/package.json` had no `typecheck` script despite `ci.yml` calling `npm run typecheck`, and the web project had zero Vitest tests despite `ci.yml` running `npm run test` — a bare `vitest run` with no test files would itself have failed the build. Added the missing script, a `vitest.config.ts` (needed to resolve the `@/*` path alias), and extracted `slugify`/`toIsoDate` — previously copy-pasted three times across pages — into `web/lib/utils.ts` + 5 real unit tests in `utils.test.ts`.
  3. **`ruff check api/` had never actually been run this session — 39 violations across nearly every file written today** (import sorting, `datetime.utcnow()` → `datetime.UTC`, one unused import, ~30 line-length violations). Auto-fixed what `--fix` could, bumped the line-length limit 100→110 (several long decorator paths and one JSON-schema description string didn't need hard-wrapping to fit under a slightly more generous limit), hand-wrapped the 5 lines still over that. `ruff check api/` is clean now.
  4. Also wired `OPENAI_API_KEY` through from `secrets.OPENAI_API_KEY` for the golden-set job — empty today so it skips exactly like local dev, but needs no workflow change once the org secret is eventually added.
- Verified locally, exactly matching the CI invocations: `ruff check api/` clean, `pytest api/ -v` → 49 passed/1 skipped, `npm run typecheck --prefix web` clean, `npm run test --prefix web` → 5 passed. API server restarted fresh post-ruff-fixes and confirmed importing/serving correctly (no accidental breakage from the auto-fixes touching 8 files).
- Remaining Product 1 Core gaps, all deliberate: **C2** (public enquiry form — lives on the same `/` Concierge route as G4/G5, genuinely blocked by the Tier-2 design gate, not just unstarted), **G4/G5** (Tier-2 gated), **I2/I3** (E2E happy-path + final `/verify` sign-off — I2 is itself blocked on G4/G5).

## 2026-07-16 — Security checklist (`.claude/agents/test-security.md`) run live for the first time

With the remaining Product 1 tasks blocked on the Tier-2 design gate and a live OpenAI key deferred by the user, ran the project-specific verification checklist that's supposed to execute on every `/verify` but had never actually been run against the live DB.

- **RLS cross-tenant denial (landlord)**: created two throwaway landlord accounts live, confirmed landlord A is blocked from spoofing landlord B's `landlord_id` on insert (403), reading/updating/deleting B's venue directly (404s — RLS returns no row rather than a permission error), and self-activating their own venue (403 — status transitions to `active` are staff-only, enforced at the RLS `WITH CHECK` level, not just in application code). Confirmed B's venue was untouched after all of A's attempts. Cleaned up both accounts + venues after.
- **Anon/public-key denial**: a true anon client (publishable key, no session at all) gets zero rows from every staff-only table (organisations, contacts, enquiries, briefs, pricing_rules, pricing_rule_addons, proposals, proposal_venues, proposal_link_tokens, user_roles, profiles) and only sees `status='active'` venues — nothing else leaks through. Every staff API endpoint correctly 401s with no token, a garbage token, or the publishable key itself used as a bearer token (i.e., you can't just hand the anon key to the API and get treated as logged in).
- **Secrets scan**: grepped every tracked file for Supabase/OpenAI/Resend key patterns and DB connection strings with embedded passwords. Every hit was documentation prose describing the *key-naming convention* (e.g. "`sb_publishable_...`"), not an actual credential. No `.env*` files tracked.
- **Pricing engine AI-call check**: confirmed `pricing_engine.py`'s only imports are `datetime` and its own schemas — no AI/LLM reference anywhere in that module or its router.
- Checked off the applicable boxes in `.claude/agents/test-security.md` directly (this is the checklist's own living document, updated as items get verified — distinct from the earlier CLAUDE.md/.claude-tooling scope decision about the Claude→GPT rename). The three remaining unchecked items (supplier RLS, Concierge rate-limiting/Turnstile, Stripe webhook verification) are marked N/A — none of that surface exists in this repo yet (Product 2/4, not built).
- Nothing broke, nothing needed fixing — this was pure verification, first time this checklist has actually been executed rather than just existing as a to-do list.
- Next: still the same fork as last entry — a live `OPENAI_API_KEY` pass (deferred by user for now), or wait for the Tier-2 brand kit to unblock C2/G4/G5/I2/I3.

## 2026-07-16 (continued) — AI provider made genuinely swappable (OpenAI/Anthropic/Gemini)

User asked whether switching AI providers later (e.g. to Anthropic or Gemini) would just be a key/variable change. Honest answer at the time: no — OpenAI's specific tool-calling wire format and exception types were hardcoded directly into `brief_parser.py`, `recommendation_engine.py`, and `copy_generator.py`, plus imported in 3 routers. Fixed properly rather than leaving it as a known gap.

- **New `api/app/core/llm.py`**: a provider-agnostic `LLMClient` protocol (`call_tool(prompt, ToolSchema) -> dict`, `generate_text(prompt) -> str`) with three adapters — `OpenAIClient`, `AnthropicClient`, `GeminiClient` — each translating the same canonical `ToolSchema` (name/description/JSON-schema parameters) into its own SDK's wire shape. `get_llm_client()` picks the adapter based on the new `AI_PROVIDER` config value (`openai`/`anthropic`/`gemini`, case-insensitive) and raises `LLMUnavailableError` naming exactly which env var is missing. Deleted the old `openai_client.py` it supersedes.
- Rewrote all three call sites (`brief_parser.py`, `recommendation_engine.py`'s rerank, `copy_generator.py`) to go through `get_llm_client()` only — none of them import `openai`/`anthropic`/`google.generativeai` anymore, confirmed by grep.
- Updated the 3 routers that previously imported `openai.APIError` directly (`briefs.py`, `recommendations.py`, `proposals.py`) to catch the new provider-neutral `LLMUnavailableError`/`LLMCallError` instead.
- `config.py` gained `ai_provider` (default `"openai"`) + `anthropic_api_key`/`gemini_api_key` alongside the existing `openai_api_key`. `.env`/`.env.example` updated to match.
- Added `api/tests/test_llm.py` (8 tests) exercising the dispatch logic itself — right provider selected per `AI_PROVIDER`, clear `LLMUnavailableError` naming the correct missing env var per provider, unknown provider rejected, case-insensitivity. Installed `anthropic` + `google-generativeai` into the dev venv so these tests exercise the real adapter classes, not mocks (production `requirements.txt` still only pins `openai` — installing the other two is a one-line addition *when* a provider switch actually happens, documented inline).
- Updated `test_golden_briefs.py`'s skip condition and `test_recommendation_engine.py`'s rerank-safety-net test to check/mock the new provider-agnostic boundary instead of OpenAI's specific response shape (the mock got dramatically simpler as a result — it now just returns a plain dict from a fake `call_tool`, not a fake OpenAI response object tree).
- **Live end-to-end proof, not just unit tests**: restarted the main dev server (still `AI_PROVIDER=openai` by default, no key) and reconfirmed the D1 smoke test still reports `OPENAI_API_KEY is not configured`. Then started a **second, throwaway** instance on port 8010 with `AI_PROVIDER=anthropic` set and no other code changes — hit its `/briefs/{id}/parse` endpoint and confirmed it now reports `ANTHROPIC_API_KEY is not configured` instead. Same server code, different config, correctly different behavior — proves the swap claim rather than just asserting it. Full pytest suite: 57 passed, 1 skipped. `ruff check api/` clean.
- **Gemini adapter is unverified** (same disclosure pattern as everything else untested against a live account this session) — written from Google's documented function-calling shape, flagged clearly in its docstring; confirm the exact `google-generativeai` surface before relying on it for real.
- Also answered a side question about venue media: there is **no AI image analysis** anywhere in the app. Photo/video captions are 100% manually typed by staff on upload, and venue search doesn't even query those captions yet (only name/district/slug). Auto-captioning via a vision model would be new work, not something partially built.
