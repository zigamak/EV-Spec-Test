# /plan

Kept as-is from the kit — copy the base command definition from the kit source.

**Purpose:** generates `specs/000N-*/plan.md` for a product. Run only after that product's schema needs are reflected in `specs/0000-foundation/erd.md` — the plan references `erd.md`, it never redefines it (setup guide §2 step 9, §3.5).
