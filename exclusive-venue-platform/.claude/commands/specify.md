# /specify

Kept from the kit, but **never run generatively** on this project (setup guide §1, §2) — same rule as `/discover`.

Paste the relevant Notion PRD content into `specs/*/prd.md` / `product-brief.md` instead.
