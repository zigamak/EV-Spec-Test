# /tasks

Kept as-is from the kit — copy the base command definition from the kit source.

**Purpose:** seeds/updates `specs/000N-*/tasks.md`. For Product 1, the seed is already provided (setup guide §8) — see `specs/0001-product-1-core/tasks.md`. For later products, decompose that week's Sprint Board rows into atomic, few-file tasks the same way.
