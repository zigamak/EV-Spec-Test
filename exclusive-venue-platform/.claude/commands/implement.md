# /implement

Kept as-is from the kit — copy the base command definition from the kit source.

**Purpose:** runs the coding agent against the next open task in `specs/000N-*/tasks.md`. Followed by `/verify` then `/track` for each task (setup guide §2 step 11).
