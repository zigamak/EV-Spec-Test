# /sdd-help

Kept as-is from the kit — copy the base command definition from the kit source.

**Purpose:** prints the pipeline order and a summary of each command. See `CLAUDE.md` for the project-specific version of this (Pipeline section) and the SDD Repo Setup Guide in Notion for full rationale.

> Note: `export-docs.md` from the base kit is deliberately **removed** on this project — the Notion + Word pipeline already covers that (setup guide §1).
