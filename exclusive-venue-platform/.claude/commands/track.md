# /track

Kept as-is from the kit — copy the base command definition from the kit source.

**Purpose:** appends the session's run log to `docs/progress.md` and `CHANGELOG.md`. Part of the weekly sync ritual: read `progress.md` + `tasks.md` checkmarks → update the Notion Sprint Board → decompose next week's rows into `tasks.md` (setup guide §7).
