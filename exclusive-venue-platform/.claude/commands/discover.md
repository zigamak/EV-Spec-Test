# /discover

Kept from the kit, but **never run generatively** on this project (setup guide §1, §2).

Paste the relevant Notion PRD/requirements content into the target spec file instead — regenerating burns hours rediscovering decisions already made. See `specs/README.md` and the paste-map in `specs/0001-product-1-core/` for what came from which Notion doc.
