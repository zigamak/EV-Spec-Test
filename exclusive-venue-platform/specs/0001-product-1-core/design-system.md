# Design System — Product 1 UI Mapping

*PLACEHOLDER — per setup guide §3.3, source is the Notion "Exclusive Venue — Design System" doc, which is Status: Not Started, blocked on brand kit delivery (due Jul 17). Populate once `/design-system` ingests the real kit.*

## Until the brand kit lands

All Product 1 UI is internal/admin-facing (`/app/*` — staff surface), so it ships on Tier 1 provisional tokens per `design-system/INTAKE.md`:

- Pipeline Board, Enquiry Command Center, Venue Library/Profile, Client, Calendar → `design-system/tokens.provisional.css`, plain shadcn/ui defaults.
- **Exception:** the Proposal Editor's *output* — the branded PDF (G4) and the public shareable link page (G5) — is client-facing and requires Tier 2 accepted tokens before build. These two tasks are explicitly gated in `tasks.md` ("G4; Tier-2 design gate", "G5; Tier-2 gate").

## Populate once brand kit ingested

- Component mapping for Pipeline Board kanban cards, Command Center layout, venue gallery, proposal template.
- Any Product-1-specific tone/voice notes for AI-drafted proposal copy (G2), beyond the shared guidance in `design-system/components.md`.
