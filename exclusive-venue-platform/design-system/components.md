# Component Library

PLACEHOLDER — populated by the design agent via `/design-system` once the brand kit lands.

Per the "Exclusive Venue — Design System" doc in Notion (Status: Not Started, blocked on brand kit, due Jul 17), this file should end up covering:

- Color palette (primary, secondary, accent — pulled from EV brand kit)
- Typography scale (headings, body, UI text)
- Spacing/grid system
- Core component library (buttons, cards, forms, tables) — likely shadcn/ui as the base per the frontend-design conventions
- Logo usage rules
- Tone/voice guidelines for proposal and Concierge copy

Until then, internal/admin surfaces build against `tokens.provisional.css` (Tier 1, `design-system/INTAKE.md`) using plain shadcn/ui defaults.
