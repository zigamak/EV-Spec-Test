# CHANGELOG

Dated ledger (git-native run history). Updated by the coding agent via `/track` — do not hand-edit history, append only.

## 2026-07-12

- Repo scaffolded per "SDD Repo Setup Guide — Exclusive Venue" (Notion, Approved, updated 2026-07-12): adjusted structure applied (`0000-foundation/`, `fixtures/`, `migrations/`, `ci.yml`; kit removals: `export-docs.md`, `mission-control/`, unused role files).
- `docs/stack-profile.md`, `memory/constitution.md`, `design-system/INTAKE.md`, `fixtures/README.md` seeded from setup guide §3–§5.
- `specs/0000-foundation/erd.md` seeded from "ERD — Cross-Product Database Schema" (Notion, Draft, 2026-07-11).
- `specs/0001-product-1-core/prd.md`, `product-brief.md`, `verification.md` seeded from "PRD — Product 1: Internal Platform (Core)" (Notion, Draft, 2026-07-11).
- `specs/0001-product-1-core/tasks.md` seeded from setup guide §8 decomposition (A1–J2, updated 12 Jul for incremental Alembic revisions).
- **A1**: `api/` FastAPI skeleton (Python 3.12) + `migrations/versions/0001_profiles_user_roles.py` — `profiles`, `user_roles`, `has_role()` helper, RLS on both.
- **A2**: `web/` Next.js skeleton (App Router, TS strict) + `/app/login` (Supabase Auth) + `web/middleware.ts` session/role gate on `/app/*`.
- **A1 (live-verified)**: `alembic upgrade head` applied revision 0001 to the Supabase project (`byupzaqlyvwssfqgryng`) via the session pooler. `api/app/core/auth.py` reworked to verify sessions via the Supabase Auth API (this project's key system has no shared JWT secret to decode locally). `migrations/env.py` normalizes the connection string to the `psycopg` v3 driver.
- Fixed a hydration mismatch on `/app/login` (browser-extension-injected input attributes) via `autoComplete` + `suppressHydrationWarning`.
- **B1 (live-verified)**: `migrations/versions/0002_venue_domain.py` — `venues`, `venue_configurations`, `venue_media`, `venue_availability`, `venue_restrictions` + RLS, applied clean to the live project. `api/app/routers/venues.py` + `api/app/schemas/venue.py` — RLS-scoped CRUD API, smoke-tested end-to-end (create/read/update/approve/delete, hold-without-expiry rejected, self-activation blocked by RLS). Upgraded `supabase` 2.9.1 → 2.31.0 (old SDK rejected this project's newer API key format as an invalid JWT).
- **B2 (live-verified)**: private `venue-media` Storage bucket (`scripts/create_storage_buckets.py`) + `api/app/routers/venue_media.py` — upload/list/update/delete with signed URLs (`api/app/core/storage.py`), DB-row-authorization-before-storage-write ordering, live-verified including a fetched signed URL matching the uploaded bytes exactly.
- **B3 (browser-verified)**: Venue Library (`web/app/app/venues/page.tsx`) + Venue Profile (`web/app/app/venues/[id]/page.tsx`), `web/lib/api/client.ts` (RLS-scoped fetch wrapper), staff nav. Added CORS to `api/app/main.py` (missing entirely until a real browser test caught it — curl-based testing can't detect CORS failures). Fixed two pre-existing strict-mode TS errors in `web/lib/supabase/server.ts` / `web/middleware.ts`. Verified with Playwright + Chromium driving the real login flow, not just curl/type-check.

## 2026-07-13

- **B4 (browser-verified)**: venue create (`/app/venues/new`) + edit (`/app/venues/[id]/edit`) forms, incl. configurations/restrictions management. No API changes — reused B1's CRUD. Added `scripts/create_test_staff_user.py` (idempotent persistent dev/test staff account, replacing the throwaway-account-per-session pattern).
- **B5/B6 (browser-verified)**: media gallery upload/reorder + availability month calendar, both added to the Venue Profile/edit pages, reusing B1/B2's existing API.
- **C1 (live-verified), revision 0003**: `organisations`, `contacts`, `enquiries` + RLS (staff-only; no anon policy — the eventual public intake path is a service-role write, not a direct anon-key insert). `api/app/routers/enquiries.py`.
- **D1/D2/D3, revision 0004**: `briefs` + RLS. `api/app/services/brief_parser.py` (Claude tool-use extraction schema) + `api/app/routers/briefs.py` (parse/list/get/patch). Deterministic confidence threshold (`AUTO_ACCEPT_THRESHOLD = 0.75`) for auto-accept vs needs-review. Golden test harness (`api/tests/test_golden_briefs.py`) + 3 synthetic fixtures in `fixtures/anonymized/`. **Not live-tested against the real Claude API — no `ANTHROPIC_API_KEY` in this dev environment**; verified instead that a missing key fails as a clean 503 and that the persist/review-patch flow around the parser works.
- **F1/F2/F3, revision 0005**: `pricing_rules`, `pricing_rule_addons` + RLS (anon has no policy at all — commercial IP). `api/app/services/pricing_engine.py::calculate_quote` — pure function, zero AI. 17 unit tests + live-verified via `/venues/{id}/pricing-rules/{id}/quote`. Built ahead of E1 (out of tasks.md's letter order) because E1's budget-fit criterion needed a real pricing engine to exist first.
- **E1/E2**: `api/app/services/recommendation_engine.py` — deterministic shortlist filter (capacity/availability/restrictions/budget, reusing F2 for real quotes) + Claude re-rank that can only reorder, never add/remove (structurally enforced and unit tested). `GET /briefs/{id}/shortlist`. 16 unit tests + live verification. Re-rank's live Claude call untested (same missing-key caveat as D1).
- **G1, revision 0006**: `proposals`, `proposal_venues` (RESTRICT FKs on venue/configuration/pricing_rules_id — a quoted venue can't be hard-deleted, verified live), `proposal_link_tokens` + RLS. `api/app/routers/proposals.py` (staff CRUD, send, link generate/revoke) + `api/app/routers/public_proposals.py` (`GET /public/proposals/{token}`, unauthenticated, service-role client validating expiry/revocation in code — no Supabase Edge Function scaffolding exists in this repo, so this plays that role). Live-verified end to end including the public token read and post-revoke 410.
- **H1**: `api/app/services/stage_machine.py` — deterministic enquiry stage transition graph (`lost` reachable from any non-terminal stage; `confirmed`/`lost` both terminal). `POST /enquiries/{id}/transition` (409 on invalid jump) + `POST /enquiries/{id}/assign`. `stage` removed from the plain `PATCH /enquiries/{id}` payload so transition is the only path that can change it. 7 unit tests (one bug caught and fixed same session: `confirmed` could initially still reach `lost`) + live verification.
- UI-only tasks deliberately left for a later pass: D4, F4, F5, G0, G2–G5, H2, J1, J2 — each already has the API surface it needs.
- **AI provider swapped: Anthropic (Claude) → OpenAI (GPT, `gpt-4o-mini`)**, at user request, across the runtime AI call path only — `api/app/core/claude.py` → `api/app/core/openai_client.py`, `brief_parser.py` (D1) and `recommendation_engine.py`'s rerank (E2) rewritten for OpenAI's function/tool-calling API, `requirements.txt`/`.env`/`.env.example` updated (`ANTHROPIC_API_KEY` → `OPENAI_API_KEY`), all tests and docs (`memory/constitution.md`, `docs/stack-profile.md`, PRD, `api-contracts.md`, `tasks.md`) updated to say GPT/OpenAI. `CLAUDE.md` and `.claude/` were deliberately left untouched — those configure the coding assistant, not the product's runtime AI. Full test suite re-verified (42 passed, 1 skipped), live 503-on-missing-key behavior re-confirmed with the new provider. **Still no live API key in this dev environment** — the actual GPT calls remain untested end-to-end, same caveat as before the swap.

## 2026-07-15

- **C3/H2/D4/G0**: Pipeline Board (kanban, replaces the placeholder) at `/app` + `NewEnquiryModal.tsx` (C3) + unified enquiry command center at `/app/enquiries/[id]` (G0, folding in D4's brief editor and H2's enquiry-detail need). Browser-verified.
- **F4/F5**: `PricingRulesTab.tsx` in the Venue edit page + `scripts/seed_demo_venues.py`, run live (3 demo venues seeded, left in the DB as standing fixtures).
- **E1 extended**: `ShortlistEntry` now carries `pricing_rules_id`/`quote_breakdown` so a proposal can be built directly from a shortlist entry.
- **G2/G3**: `api/app/services/copy_generator.py` + two GPT copy-generation endpoints; proposal editor at `/app/proposals/[id]` (intro/venue copy, send, shareable-link create/revoke). Full shortlist→proposal round trip browser-verified.
- **J1/J2**: read-only client list/profile (`/app/clients`) and portfolio-wide availability calendar (`/app/calendar`), built entirely from existing tables.
- **Bug fix**: `web/lib/supabase/client.ts` created a new Supabase client per `apiFetch()` call — harmless until a page fired 3+ sequential calls (the proposal editor, G3, was the first to), at which point supabase-js's cross-instance session-lock serialization (Web Locks API) silently hung later calls forever with no error. Fixed with a module-level client singleton.
- Not built this session: C2, C4, G4/G5 (Tier-2 design gate), I1–I3 (CI/E2E/verify).

## 2026-07-15 (continued)

- **C4**: `POST /webhooks/resend/inbound-email` — Svix HMAC signature verification (`app/services/webhook_verification.py`, 5 unit tests), fails closed (503/401) on missing secret or bad signature. Live-verified with a self-signed payload. Payload shape unverified against a real Resend account (docs-only, no live account configured).
- **I1**: `ci.yml` had two latent gaps fixed before they could fail a real run — no `auth` schema in the plain-Postgres CI service (added a minimal bootstrap: stub `auth.users` + `auth.uid()`), and `web/` had no `typecheck` script nor any Vitest tests despite both being wired into the workflow (added the script, `vitest.config.ts`, and `web/lib/utils.ts`/`utils.test.ts` — also de-duplicating `slugify`/`toIsoDate` from three call sites). `OPENAI_API_KEY` wired through from repo secrets for the golden set.
- **39 ruff violations fixed** across the session's Python code (first time `ruff check api/` had actually been run) — import order, `datetime.UTC`, one unused import, line length (bumped 100→110, hand-wrapped the rest).
- Confirmed the previous session's Supabase browser-client singleton fix is complete and correctly scoped; no further changes needed.

## 2026-07-16

- Security checklist (`.claude/agents/test-security.md`) run live for the first time: RLS cross-tenant denial (landlord A blocked from reading/writing/self-activating landlord B's venue), anon-key denial across every staff-only table, secrets-scan (clean), pricing-engine AI-import check (clean). Nothing broken, nothing needed fixing.
- **AI provider made genuinely swappable** (`AI_PROVIDER=openai|anthropic|gemini`, one env var, no code changes): new `api/app/core/llm.py` (`LLMClient` protocol + `OpenAIClient`/`AnthropicClient`/`GeminiClient` adapters + `get_llm_client()` factory), replacing the OpenAI-specific wire format previously hardcoded into `brief_parser.py`, `recommendation_engine.py`, `copy_generator.py`, and 3 routers. Deleted `openai_client.py`. Live-proved (not just unit-tested): a second throwaway instance on port 8010 with `AI_PROVIDER=anthropic` correctly reported the Anthropic key as missing instead of the OpenAI one. `test_llm.py` (8 tests). Gemini adapter is unverified against a live account (documented in its own docstring).
- **Visual reskin toward client reference screenshots** (reference only, not the accepted Tier-2 kit — `design-system/INTAKE.md` gate unchanged): `design-system/tokens.provisional.css` (navy/deep-red palette), Playfair Display + Inter via `next/font/google`, staff shell sidebar, `/app/login`, Pipeline Board, Venue Library, portfolio Calendar. `avatarColorForId`/`daysSince` added to `web/lib/utils.ts`.
- **Real persistent logging**: `api/app/core/logging_config.py` (rotating file handler, `api/logs/app.log`, also captures uvicorn access/error logs) + a global unhandled-exception handler in `main.py`.
- **Bug fix**: a Windows + `uvicorn --reload` keep-alive connection bug caused silent request hangs (`OPTIONS` preflight succeeding, matching `GET` never arriving) — fixed with a dev-only `Connection: close` response header, plus a client-side `fetchWithTimeout`/`fetchWithRetry` fallback in `web/lib/api/client.ts`.
- **Bug fix (major)**: Supabase Auth API was being re-verified on every request instead of cached — added a 30s in-process TTL cache keyed by bearer token in `api/app/core/auth.py` (`test_auth_session_cache.py`, 4 tests); cut Auth API calls by ~90% (15+ calls → 1 across three page loads, confirmed via logs).
- **N+1 query batching** (the remaining latency source — each individual Supabase call is 2-5s, plausibly geographic given the project is hosted in `aws-0-eu-central-1`): `GET /enquiries` now embeds every brief version per enquiry via PostgREST relationship syntax; new `GET /venues/portfolio-availability` embeds availability windows per venue; new `GET /venues/portfolio` embeds media (with signed URLs) + configurations per venue. Fixes the Pipeline Board, Calendar, and Venue Library pages' worst N+1 offenders. New schemas (`EnquiryWithBriefs`, `VenueWithAvailability`, `VenueWithPortfolio`) on both the Pydantic and TypeScript sides. Live-timed: new batched `/enquiries` ~2-3s vs. the old per-enquiry `/briefs` loop ~4-4.5s against the same data, consistent across repeated runs.
- Full suite re-verified: `pytest api/` 61 passed/1 skipped, `npm run typecheck --prefix web` clean, `npm run test --prefix web` 9 passed.

## 2026-07-18

- **Enquiry stage-model conflict resolved** — the live `enquiries.stage` implementation (8 stages: new/qualified/proposal_sent/follow_up/visit/negotiation/confirmed/lost, `stage_machine.py`, task H1) vs. the reference prototype's simpler Open/Awaiting/Won model (Notion "Reference Prototype Workflow — Inquiry to Proposal", exclusive-venue-internal-ai-sales.netlify.app, ownership unconfirmed). Decision: the 8-stage pipeline stays canonical (already built, tested, live); the reference's simpler model is layered on top as a computed Open/Awaiting/Won/Lost status rollup, not a schema change. Full stage-by-stage trigger table and the reconciliation of the rest of the reference-workflow comparison (decline/forward actions, pricing-lock UX, delivery-email prompt, post-send tracking) documented in `specs/0000-foundation/erd.md` §5.1. `specs/0001-product-1-core/prd.md` §4/§5 updated to match. New follow-up task **H3** logged in `tasks.md` (proposal-send → stage-transition auto-coupling is currently two separate manual staff actions; status-rollup helper not yet built).
- Mirrored in Notion: PRD — Product 1 §4/§5, and the "Reference Prototype Workflow — Inquiry to Proposal" comparison table (Pipeline stages / Decline-Forward rows marked resolved).

## 2026-07-18 (continued)

- **H3 (partial):** status-rollup helper shipped — `stage_to_status()` / `STAGE_TO_STATUS` in `api/app/services/stage_machine.py`, exposed as a computed `status` field on every `Enquiry`/`EnquiryWithBriefs` API response (no schema change, no migration). `GET /enquiries` gained a `pipeline_status` filter (named to avoid shadowing FastAPI's `status` import). Pipeline Board (`web/app/app/page.tsx`) now shows Open/Awaiting/Won/Lost summary pills above the kanban columns, clickable to filter; "Lost" — which has no kanban column by design — renders as a flat list instead of an empty board. 5 new unit tests (`test_stage_machine.py`, 12 total, all passing); `npm run typecheck` clean. Proposal-send auto-coupling (the other half of H3) still not done.

## 2026-07-18 (stage-model revamp: 8-stage → 5-stage)

- **Full pipeline stage model replaced**, superseding the same-day resolution above: fuller workflow context (the complete Inquiries → Proposal → Pipeline → Client journey, including the 4-step proposal builder and an explicit 5-stage Kanban) made clear the intended pipeline is **Enquiry → Briefed → Proposed → Held → Signed** (plus a hidden terminal `lost`), not the original 8-stage build. User explicitly chose to replace, not relabel, via `AskUserQuestion`.
- **`migrations/versions/0007_enquiry_stage_revamp.py`** (new file, task H4 — does not edit revision 0003 per the immutable-revision rule): remaps existing rows from the 8 old stage values to the 5 new ones + lost (`new`→`enquiry`, `qualified`→`briefed`, `proposal_sent`/`follow_up`→`proposed`, `visit`/`negotiation`→`held`, `confirmed`→`signed`, `lost`→`lost`), drops the old CHECK constraint via a dynamic `pg_constraint` lookup (no hardcoded guessed name), adds an explicitly-named `enquiries_stage_check` with the 6 new values, sets the column default to `'enquiry'`. Includes a documented lossy `downgrade()`. **Not yet applied to any live database — no Supabase credentials available in this environment.** Must be run via `alembic upgrade head` against the real project before any of the following work functions against live data.
- **`api/app/services/stage_machine.py`** rewritten: `ALLOWED_TRANSITIONS` now `enquiry→briefed→proposed→{held,signed}`, `held→{signed,proposed}` (a lapsed soft hold drops back to `proposed`, not lost), `lost` reachable from any non-terminal stage. `STAGE_TO_STATUS` remapped: Open (enquiry, briefed), Awaiting (proposed, held), Won (signed), Lost (lost). 13 unit tests (fully rewritten), all passing.
- **`api/app/schemas/enquiry.py`** — `EnquiryStage` literal updated to the 6 new values; `Enquiry.status` computed field unchanged in mechanism.
- **`api/app/routers/enquiries.py`** — docstrings/examples updated to the new stage names; `pipeline_status` filter logic unchanged (already generic over `STAGE_TO_STATUS`).
- **`web/lib/api/types.ts`** — `EnquiryStage`, `ENQUIRY_STAGES`, `STAGE_LABEL`, `STAGE_TO_STATUS` all updated to the 5-stage model. `web/app/app/page.tsx` (Pipeline Board) required **no changes** — it's fully generic over the exported stage constants, confirmed via `npm run typecheck` and a grep for hardcoded old stage-name literals.
- **`scripts/seed_demo_enquiries.py`** rewritten for the new stage names (3 open / 2 awaiting / 1 won / 1 lost across 7 demo enquiries). Logic re-verified in this environment via an in-memory fake Supabase-client harness: all 7 rows created with the correct stage/status distribution, second run correctly skips all 7 as already-existing (idempotency confirmed). **Still not run against a real Supabase project** — same missing-credentials caveat as migration 0007.
- **Docs updated to match:** `specs/0000-foundation/erd.md` §5 (enquiries CHECK description) and §5.1 (full lifecycle rewrite, explicitly marked as superseding the earlier same-day resolution) + §10 build order (migration 0007 added to the revision map); `specs/0001-product-1-core/prd.md` §4 step 7, §5 Enquiry intake/CRM pipeline/Stage model decision, and the Pipeline Board page-inventory row; `specs/0001-product-1-core/tasks.md` H1 rewritten for the new stage names, H3's stale "superseded pending a bigger decision" note updated to reflect the decision is made and executed, new task **H4** added for migration 0007.
- **Notion mirrored:** "PRD — Product 1: Internal Platform (Core)" (§4 step 7, Enquiry intake, CRM pipeline, Stage model decision, Pipeline Board page-inventory row) and "Reference Prototype Workflow — Inquiry to Proposal" (the "Pipeline stages" comparison-table row and the "Resolution" note at the bottom) both updated to describe the final 5-stage decision instead of the now-superseded 8-stage one.
- **Full verification pass:** `pytest api/tests/test_stage_machine.py` — 13/13 passed. `ruff check` on every changed Python file — clean. `npm run typecheck --prefix web` — clean. Seed script dry run (fake client) — correct distribution + idempotent re-run confirmed. **Outstanding for the user:** run `alembic upgrade head` against the live Supabase project, then `python scripts/seed_demo_enquiries.py`, before testing the Pipeline Board against real data.
- New `scripts/seed_demo_enquiries.py` (mirrors `seed_demo_venues.py`'s idempotent-by-unique-field pattern, keyed on contact email): 7 demo enquiries spanning every stage (3 Open, 2 Awaiting, 1 Won, 1 Lost with a reason), each with a brief, so the Pipeline Board shows a realistic multi-status spread. Verified in this sandbox with a fake Supabase-client stand-in (control flow, schema/CHECK-constraint compliance, idempotency on a second run) — **not yet run against a live Supabase project**, no credentials available in this environment.

## 2026-07-18 (standardized brief contract, task D5)

- **Live-browsed the reference prototype** (exclusive-venue-internal-ai-sales.netlify.app, via the Chrome extension, not just Notion's paraphrase of it) to see the actual Concierge briefing UI on a real Dior enquiry: client tier + rate card, a date *window* with two suggested dates, a "TBC" budget with an AI-estimated range, mood/format/tech needs, and "96% confidence, 1 field flagged" — a level of detail the original `briefs` table (0004: a single `event_date` + one nullable `budget_amount`) couldn't represent. Also confirmed live: the reference's own Pipeline board literally uses our column names (Enquiry/Briefed/Proposed/Held), with the terminal column labeled "Won" rather than "Signed" — noted as a naming nuance, not changed (we still split stage=signed from a computed status=won).
- **Decision: standardize on one brief contract that every intake channel targets** — email, WhatsApp, or a manual note through the AI parser; the eventual public web form (C2) filling the same columns directly and deterministically, skipping the AI parser entirely since a client's own structured form answers aren't a parsing problem.
- **`migrations/versions/0008_brief_standardization.py`** (new file, task D5 — does not edit 0003/0004/0006 per the immutable-revision rule): `briefs.event_date` renamed to `date_window_start` (data-preserving rename) + new `date_window_end` (backfilled from the renamed column so no row is left half-open), `date_suggestions`, `time_of_day`, `budget_status` (backfilled `'confirmed'` where `budget_amount` was already set), `budget_estimate_low/high`, `flagged_fields`, `fields_to_confirm`. `enquiries.channel` and `contacts.source` CHECK constraints replaced (same dynamic `pg_constraint`-lookup pattern as 0007) to add `'whatsapp'`. `organisations` gains `tier`, `rate_card_on_file`, `rate_card_terms`, `region`. `proposals` gains `event_date` (the *locked* date decided at generate-and-share, distinct from the brief's window) and `personal_email_copy` (the AI-drafted note that accompanies a sent proposal — a real reference-site feature, not a backlog guess). Full documented `downgrade()`. **Not yet applied to any live database** — no Supabase credentials in this environment, same caveat as 0007.
- **`api/app/schemas/brief.py`** rewritten: `ParsedBrief`/`BriefUpdate`/`Brief` now carry the full standardized field set, plus two new model-level validators (`date_window_end >= date_window_start`; `budget_estimate_low`/`high` both-or-neither with `high >= low`) — both directly exercised in this sandbox and confirmed to raise correctly. `requirements` stays free-form jsonb with a *documented* conventional shape (`format_needs`/`tech_needs`/`mood`/`attachments`, `REQUIREMENTS_CONVENTIONAL_KEYS`) validated in code, not a DB CHECK — same trade-off `pricing_rules`' jsonb tiers already make.
- **`api/app/services/brief_parser.py`**'s `EXTRACT_BRIEF_TOOL` (the GPT tool-calling schema) rewritten field-for-field to target the new contract, with per-field descriptions instructing the model when to use `flagged_fields` vs. `fields_to_confirm` vs. lowering `confidence`.
- **`api/app/schemas/enquiry.py`**: `EnquiryChannel`/`ContactSource` gain `'whatsapp'`; `Organisation`/`OrganisationCreate`/`OrganisationUpdate` gain `tier`/`rate_card_on_file`/`rate_card_terms`/`region` (lifetime value/win rate/open-proposal-count deliberately NOT added — computed from `proposals`/`enquiries` at query time, same pattern as `enquiries.status`).
- **`api/app/schemas/proposal.py`**: `Proposal`/`ProposalCreate`/`ProposalUpdate` gain `event_date`/`personal_email_copy`.
- **`api/app/routers/recommendations.py`**: `_load_brief` now reads `date_window_start` (the effective single date for availability/pricing until a proposal locks a real one) instead of the removed `event_date` column.
- **`api/app/routers/proposals.py`**: intro-copy generation now prefers the proposal's own locked `event_date`, falling back to the brief's `date_window_start`.
- **`scripts/seed_demo_enquiries.py`** rewritten: all 7 entries' briefs use the new field names; the Luxe & Co. entry is now a full standardized-brief showcase (tier, rate card, date window + two suggested dates, TBC budget with an estimated range, mood/format/tech needs, flagged fields) mirroring the real Dior enquiry observed live; David Ho's entry switched to the `whatsapp` channel to prove the contract really is channel-agnostic, not just email-shaped. Re-verified with the fake-client harness: correct field population, correct channel distribution, idempotent on re-run.
- **`fixtures/anonymized/*.json`** (the golden brief-parser test fixtures) updated for the renamed fields (`event_date` → `date_window_start`/`date_window_end`, `budget_status` added where a firm figure was already expected).
- **Frontend**: `web/lib/api/types.ts` (`Brief`, `BriefUpdate`, `Organisation`, `OrganisationCreate`, `Proposal`, `ProposalCreate`, plus new `BudgetStatus`/`TimeOfDay`/`OrganisationTier`/`BriefRequirements` types) updated to match. Every UI reference to `brief.event_date` — Pipeline Board card, Calendar's "upcoming events" sidebar (both the filter/sort logic and the display), and the Command Center's brief editor form (now two date inputs, window start/end) — updated accordingly.
- **Full verification pass:** `pytest api/` — 63 passed, 1 skipped (golden briefs, no API key), 4 pre-existing failures unrelated to this change (missing `openai`/`anthropic`/`google-generativeai` packages in this sandbox — a known environment limitation, not a regression). `ruff check` across every changed file — clean. `npm run typecheck --prefix web` — clean. Seed script dry run — correct field population + idempotent re-run confirmed. `ParsedBrief`'s two new validators exercised directly (valid case, bad date-window-order case, half-open-budget-range case) — all three behaved correctly. **Outstanding for the user:** run `alembic upgrade head` against the live Supabase project (0007 and 0008 both still pending), then `python scripts/seed_demo_enquiries.py`, before testing against real data.
