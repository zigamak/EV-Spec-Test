Kept as-is from the AI-Spec-Driven-Development-Flow kit (setup guide §1). The pipeline commands (.claude/commands/) copy scaffolding from here — populate with the kit's original template files (spec/plan/tasks templates, etc.).
